# agent-ai-gateway

AI gateway infrastructure for the agent setup.

The Docker stack lives in:

```text
src/
```

Copy `src/.env.example` to `src/.env` on the NAS, fill real
secrets there, and do not commit `.env`.

See `src/README.md` for the runbook.
